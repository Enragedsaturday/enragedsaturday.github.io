# S9 P4 escalation — callout↔registry "deep-equals" gate (spec text vs build convention)

**Filed:** 2026-07-21 (P4, RULING P4-04). **Adjudicator:** claude-fable-5-orchestrator.

S9 R10(a) requires every S5 R2 rule-callout paragraph to **deep-equal** its registry point
`statement` (S3 R4). The COH packet measured the entire corpus: **0/80 registry statements are
verbatim-equal** to their home-page callouts. The convention actually built (S3→S5→S7 era) is a
hand-edited paraphrase in the registry with the page callout as the authoritative prose.
Taxonomy: 2 cosmetic · 61 cite-divergence · 14 content-divergence · 3 no-callout.

**Dispositioned in P4:** the 14 content-divergences + 3 no-callouts are being individually
reviewed (Wave F, packet COH-B); the 61 cite-divergences get class treatment (registry-note vs
page-fix per side). Artifacts: `_run/s9/p4/out/COH-report.json`, `COH-findings.jsonl`.

**The escalated question (user decision at P5):** does the R10(a) "deep-equals" sentence stand
as written — requiring a mass rewrite of one side (80 statements or 80 callouts) — or is the
built convention (semantic equivalence + citation consistency, paraphrase allowed) adopted as a
spec amendment? P4 did NOT rewrite verified content to satisfy a gate the build never
implemented (P3 rekey-unsanctioned precedent). Recommendation: adopt the built convention via
spec amendment; the P4 review of the 17 substantive rows covers the real risk.
