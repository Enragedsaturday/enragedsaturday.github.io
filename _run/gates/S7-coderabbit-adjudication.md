# S7 CodeRabbit gate — adjudication + fix record

- gate artifact: `_run/gates/S7-coderabbit-66d8f79.md` (CodeRabbit CLI 0.6.4, 11 findings, all `major`)
- commit under review: 66d8f79 (base ef0adfa)
- adjudicator: `{lane: s7-coderabbit-gate-adjudication, model: claude-opus-4-8}`
- discipline: S6 gate precedent (find → adjudicate on the merits → fix upheld → verify). Writer ≠ approver: this record + the fixes are returned for the orchestrator to adjudicate/commit; I commit nothing.

## Verdict summary

**11 UPHELD · 0 REFUTED · 0 deferred (findings).**
One upheld finding (#9) was fixed with a **corrected** patch — the reviewer's literal proposal was too narrow and would have introduced a false-positive regression (proven with live evidence); the underlying defect is real and is fixed. One **out-of-scope observation** (pre-existing worklist/survey staleness, not a CodeRabbit finding and not a code defect) is registered for the orchestrator, below.

Every finding was examined for refutability rather than rubber-stamped; each is a genuine fail-closed / correctness gap under the `scripts/` path instructions ("errors must never pass silently as success"; "flag any path where an exception or empty result could be recorded as a verified state"; "API-quota safety"; "correctness of comparison/normalization logic"; "resumability/idempotence").

## Per-finding table

| # | File · span | Finding (summary) | Verdict | Evidence | Fix (commit-ready) |
|---|---|---|---|---|---|
| 1 | `scripts/s2/ingest.py` 5279-5284 (+5319-5320) | Refuse panel re-keys that produce no citation display | **UPHELD** | `classify_citations()` returns `display=None` whenever `select_official_cite` picks nothing (e.g. only vendor-neutral/type≠1 or unlisted-reporter cites), yet `block["all"]` still contains `expect_cite` — so the expect-cite guard passes, `record["citations"]=block` + `expected_citation_found=True` is written, but `if block.get("display")` skips the manifest update, leaving `row["official_cite"]` stale/desynced. Empty result recorded as verified state. | In `rekey_cluster_panel`, added a `if not block.get("display"): raise SystemExit(...)` guard immediately after the expect-cite guard (fail closed before any mutation); made `row["official_cite"] = block["display"]` unconditional (guaranteed present). Self-test: new type-8 no-display target cluster 8888888 → SystemExit, cluster_id + official_cite unchanged. |
| 2 | `scripts/s2/ingest.py` 5099-5103 | Fail closed on malformed cached `sub_opinions` | **UPHELD** | `extract_opinion_id()` raises `ValueError` on a dict carrying `cluster_id` but no opinion id; the `except ValueError: continue` silently drops it, so a corrupt/partially-parsed head element lets a LATER opinion id become the harmonized lead from incomplete ordering, written as a verified re-key. | `harmonized_lead_from_cluster` now re-raises `ValueError("cached cluster … unparsable sub_opinion")`. Callers catch it: `rekey_lead_opinion_from_cache` → journal `queued-for-lane` (reason "unparsable cached sub_opinion") + queue (never write); `rekey_cluster_panel` → `SystemExit` (abort, no write). Self-tests: `malformed-sub--950` → queued (lead untouched); panel target 7777777 → SystemExit. |
| 3 | `scripts/s2/ingest.py` 11654-11656 | Reject panel companion args without `--rekey-cluster-panel` | **UPHELD** | `--rekey-cluster-target/-expect-cite/-evidence` set without `--rekey-cluster-panel` matches no dispatch branch in `run_ingest`, so it falls through to normal live ingest and can consume CL budget. Exactly mirrors the existing `--repair-coa-state-allow-docket-fetch` guard (line 6678). | Added a fail-closed guard in `run_ingest` next to the coa-state guard: raises `SystemExit("--rekey-cluster-target/…/-evidence require --rekey-cluster-panel")`. Smoke-verified: `ingest.py --rekey-cluster-target 2680439` → exit 1 with that message (no CL touch). |
| 4 | `scripts/s2/ingest.py` 5121-5150 | Move lead normalization into phase-1 prevalidation | **UPHELD** | Phase-1's comment promises whole-batch validation before any write, but `int(current_lead)` (and the bare `int(cluster_id)`) ran in phase 2 (line 5150), so a non-integer `identity.lead_opinion_id` raised mid-phase-2 *after* earlier batch rows were already written — a partial write + crash, breaking the all-or-nothing contract. | Phase 1 now wraps both `int(cluster_id)` and lead normalization in `try/except → SystemExit`, and carries the normalized `current_lead` into the `targets` tuple; phase-2 unpacks it and no longer recomputes. Self-test: `bad-lead--777` (`lead_opinion_id="not-an-int"`) batched with a valid row → SystemExit before any write. |
| 5 | `_overhaul2/scripts/build_coverage_ledger.py` 339-403 | Missing S7 disposition files silently skipped, no failure signal | **UPHELD** | The three `if os.path.isfile(path)` blocks (L1-pooling / L2-SACO / RL) have no else/warning/count; the `ok` gate (L620) ignores whether they ran. All three files exist and are **git-tracked committed inputs** (`_run/o2-execute/S7-{L1-POOLING,L2-SACO,RL}-DISPOSITIONS.jsonl`), so a renamed/deleted/upstream-failed file would quietly shrink the ledger and still print PASS. | Record `s7_stage_status[name] = os.path.isfile(path)` for each; print a presence line; fold `not s7_missing` into `ok` (a missing required file now FAILS the run); surface `s7_dispositions_present` in `out["counts"]`. **Measurement impact (explained):** the committed ledger gains exactly one counts key (`s7_dispositions_present`, all-`true`); regen is otherwise byte-identical (5-line add, 0 removed) and still `RESULT: PASS`. |
| 6 | `scripts/s7/survey.py` 475-486 | Fail closed when the survey is empty | **UPHELD** | Zero substantive pages would write a valid-looking JSON and exit 0, letting a discovery/path regression record as success. | Added `require_nonempty_survey(survey)` (raises `RuntimeError` on `totals.pages == 0`), wired into `main`. Deterministic (no effect on a non-empty corpus). Self-test: empty→raises, non-empty→passes through. |
| 7 | `scripts/s7/survey.py` 480-486 | Reject missing `--out` value and bound the path | **UPHELD** | A bare `--out` (no following token) silently fell back to the default output (fail-closed violation); a relative `--out ../..` escaped `REPO_ROOT`. | Added `resolve_out_path(argv, default)`: raises `ValueError` on a bare flag; joins `REPO_ROOT` for relative; `realpath`-bounds the result under `REPO_ROOT`. Determinism preserved — the canonical regen (no `--out`) resolves to the identical default path. Self-tests: default / relative-under-root / bare-flag-raises / relative-escape-raises / abs-escape-raises. |
| 8 | `scripts/s7/build_worklist.py` 283-306 (+431-435) | Preserve T2 resolution failures instead of collapsing to absent/special | **UPHELD** | For `__INPAGE/__PARENT/__CROSSREF` specs, `cur = hits[0] if len(hits)==1 else None` while `kind` stayed the special label, so an ambiguous/absent host was counted in the "in-page/retained-parent/cross-ref materialized" subtotal (L431) even with `exists=False` — internally inconsistent (also double-counted as absent). Normal-node ambiguity collapsed to "absent", masking it. | Added `resolve_unique_basename(by_base, name) → (path|None, resolution)` (`basename-match` / `AMBIGUOUS:…` / `absent`); special branches now set `kind = "<special>" if cur else resolution`; the else-branch uses `classify_t2_state(row) if cur else resolution`. Output-neutral on the current tree (all specials resolve uniquely). |
| 9 | `scripts/s7/build_worklist.py` 223-263 | Validate split-landing overrides before marking T1 rows resolved | **UPHELD (fix refined)** | Real defect confirmed live: `resolve_t1_current` returned the hardcoded `T1_SPLIT_LANDINGS[n]` as `resolved:true` even when it had no survey row, rendering "resolved" alongside `(no survey row)` flags. **However** the reviewer's literal `if cur in by_path` check is too narrow: rows 6 & 8 landings (`…/two-definitions-of-search/index.md` 356w, `…/third-party-…/index.md` 408w) EXIST on disk and are correctly re-homed, but fall below the survey's 400-word substantive threshold → excluded to `partition.excluded_index_stubs`, absent from `by_path`. The literal patch flipped them to `override-missing`/unresolved (45→43), a false regression. | Fix validates the override against the **full page universe the survey visited** = `by_path ∪ excluded_index_stubs ∪ excluded_named` (real pages, whether surveyed-substantive or stub-excluded), not just substantive `by_path`. A genuinely renamed/deleted landing is in none of these → `override-missing`, fail-closed; a stub-excluded-but-real landing stays resolved. **Output-neutral** vs original code on the current tree (T1 45/48; rows 6/8/28 all resolve). |
| 10 | `scripts/s7/build_worklist.py` 456-486 | Reject unknown CLI arguments | **UPHELD** | `main` tested only `"--stdout" in argv`; a typo (`--stdotu`) was silently ignored and fell through to writing output files. | Replaced ad-hoc parsing with `argparse.ArgumentParser` (`--stdout` store_true). Unknown flags now exit non-zero. Verified: `--stdotu` → exit 2 (unrecognized); no-arg and `--stdout` unchanged. |
| 11 | `scripts/s7/build_worklist.py` 487-491 | Write generated outputs atomically | **UPHELD** | Direct `open(...,"w")` writes can leave a truncated `s7-worklist.json` / `S7-WORKLIST.md` on interrupt, weakening rerun/resume safety (path instruction: prioritize idempotence). | Added `atomic_write(path, text)` (temp file + `os.replace`); both artifacts routed through it. Byte-identical content to the direct write (proven output-neutral vs original code); no leftover `.tmp`. |

## Verification results

| Check | Result |
|---|---|
| `python3 scripts/s2/ingest.py --self-test` | **PASS** (exit 0; extended: malformed-sub queue, non-integer lead abort, no-display panel target, unparsable panel sub_opinion) |
| `python3 scripts/s7/survey.py --self-test` | **PASS** (exit 0; extended: empty-survey guard, `--out` resolution — bare-flag + escape rejection) |
| canonical survey regen (`survey.py`) byte-comparable | **byte-identical** to committed `_run/o2-execute/s7-survey.json` (code-only fixes, no measurement delta) |
| `python3 _overhaul2/scripts/build_coverage_ledger.py --write` | **PASS** — ledger delta = single explained counts key `s7_dispositions_present` (all `true`); otherwise byte-identical |
| `python3 scripts/build_case_index.py` idempotent | exit 0; `Case Index.md` md5 unchanged before/after (`5b5616d…`); no new churn on the orchestrator's uncommitted regen |
| `python3 scripts/lint/run_all.py` | **byte-identical to pre-edit baseline** (6828 finding lines; per-code counts unchanged; exit 1 = same pre-existing HIGH backlog). LINT-13/15/16/17 unchanged (0 findings each, as baseline). |
| `npx quartz build` | **exit 0** (724 markdown parsed, 2873 files emitted) |

## Registered for the orchestrator (out-of-scope observation, not a CodeRabbit finding)

**Pre-existing worklist/survey staleness — NOT caused by these fixes, NOT a code defect.** The committed `_run/o2-execute/s7-worklist.json` and `S7-WORKLIST.md` are a planning snapshot generated from an EARLIER survey: running the **original, unmodified** `build_worklist.py` against the committed `s7-survey.json` reproduces a 1774-line JSON / 220-line MD diff vs the committed worklist (T1 pages that were `verified`/no-H1/em-dashy in the snapshot are now `draft`/H1/0-em in the current tree). The committed survey already reflects the current tree; the committed worklist does not. I restored both artifacts to their committed state (my `build_worklist.py` code changes are proven output-neutral) rather than fold a large unreviewed measurement refresh into a code-gate close. Recommendation: the orchestrator decide whether/when to regenerate the worklist (a measurement decision), separate from this gate.

## Git scope of my edits

- `scripts/s2/ingest.py` — findings 1,2,3,4 + extended self-tests
- `scripts/s7/survey.py` — findings 6,7 + extended self-test
- `scripts/s7/build_worklist.py` — findings 8,9,10,11
- `_overhaul2/scripts/build_coverage_ledger.py` — finding 5
- `_run/s6-coverage-ledger.json` — finding-5 regen (single explained key)
- `_run/gates/S7-coderabbit-adjudication.md` — this artifact

**Left untouched (orchestrator's / not mine):** `_overhaul2/lake/_schema.json` (M, orchestrator schema-enum fix), `content/legal-system-research-and-reference/Case Index.md` (M, orchestrator regen — re-run was idempotent), the 3 untracked S7 close artifacts, and the gate artifact. No commits made.
