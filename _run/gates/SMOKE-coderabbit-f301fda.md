# CodeRabbit gate — SMOKE @ f301fda (base: a4c09c0)

- run: 2026-07-05T23:29:49Z
- cli: 0.6.0
- mode: --plain --type committed --base-commit a4c09c0
- scope: .coderabbit.yaml path filters (code only)

```
Notice: Detected claude environment. Use `coderabbit review --agent` for structured agent-friendly output.

────────────────────────────────────────
CodeRabbit CLI 0.6.0
What's new

Review       : coderabbit review --fast for faster local reviews
Auth         : clearer sign-in, organization, and rate-limit context in coderabbit auth status
Deprecated   : --interactive and --prompt-only; use plain mode or coderabbit review --agent
Fix          : Ctrl-C exits review/auth cleanup promptly
Fix          : repository matching now works across SSH, self-hosted, and worktree setups
────────────────────────────────────────

╔═══════════════════════════════════════════╗
║                                           ║
║   New update available! 0.6.0 -> 0.6.4    ║
║          Run: coderabbit update           ║
║                                           ║
╚═══════════════════════════════════════════╝

Connecting to CodeRabbit... 0s elapsed
Preparing review... 2s elapsed
────────────────────────────────────────
CodeRabbit Review

Diff      : committed changes only
Compare   : HEAD → main
Directory : cr-gate-SMOKE-J0Qwqy
────────────────────────────────────────

(\(\
(• .•)  I see you chose the 'dynamic typing' difficulty setting. On purpose.

Preparing sandbox... 3s elapsed
Summarizing changes... 7s elapsed
Finishing analysis tools... 37s elapsed
Writing review comments... 37s elapsed
Writing review comments... 1m 00s elapsed - still working
Writing review comments... 2m 00s elapsed - still working

────────────────────────────────────────────────────────────────────────
  major [potential_issue]
  → ]8;;vscode://file//private/var/folders/vr/4ydf2jp510ldxk10ghhvqm2h0000gn/T/cr-gate-SMOKE-J0Qwqy/scripts/s2/ingest.py:650scripts/s2/ingest.py:650-671]8;;

  party_term_candidates only maps abbreviation → full form, not full →
  abbreviation.

  The reverse-lookup at Line 654-658 only adds a candidate when the stripped
  term itself equals a contraction value (i.e., the extracted term is
  already abbreviated, e.g. "assn" → adds "association"). It never
  handles the mirror case where the extracted party term is the fully
  spelled-out form (e.g. "association") but the opinion text only contains
  the abbreviated form ("ass'n"/"assn"). In that scenario no candidate
  for the abbreviated form is ever generated, so missing_party_terms would
  incorrectly flag the term as missing — exactly the kind of false negative
  this cohort is meant to eliminate, just in the opposite direction.


  🔧 Proposed fix to add the missing direction

   def party_term_candidates(term):
       lowered = str(term or "").lower()
       stripped = strip_apostrophes(lowered)
       candidates = {candidate for candidate in (lowered, stripped) if candidate}
       candidates.update(
           full
           for full, contraction in CAPTION_TOKEN_CONTRACTIONS.items()
           if stripped and contraction == stripped
       )
  +    if stripped in CAPTION_TOKEN_CONTRACTIONS:
  +        candidates.add(CAPTION_TOKEN_CONTRACTIONS[stripped])
       return candidates


────────────────────────────────────────────────────────────────────────
  major [potential_issue]
  → ]8;;vscode://file//private/var/folders/vr/4ydf2jp510ldxk10ghhvqm2h0000gn/T/cr-gate-SMOKE-J0Qwqy/scripts/s2/ingest.py:662scripts/s2/ingest.py:662-671]8;;

  Opinion text isn't apostrophe-normalized before substring matching.

  lowered = (text or "").lower() at Line 666 only lowercases the text; it
  never strips apostrophes. If the opinion text contains the abbreviated
  party term with a different apostrophe glyph than the one used in the
  case-name-derived term (e.g. case name has straight ' but extracted
  opinion text has a curly ’, a very common mismatch between metadata
  captions and OCR'd/typeset opinion text), none of the generated candidates
  will match: "assn" is not a substring of "ass’n" because the curly
  apostrophe still separates the letters. This leaves the exact class of bug
  this cohort targets unresolved for the text side of the comparison.


  🔧 Proposed fix: normalize apostrophes on the text side too

   def missing_party_terms(case_name, text):
       terms = first_party_terms(case_name)
       if not terms:
           return []
  -    lowered = (text or "").lower()
  +    lowered = strip_apostrophes((text or "").lower())
       return [
           term
           for term in terms
           if not any(candidate in lowered for candidate in party_term_candidates(term))
       ]

  Note this pairs with the stripped candidate already generated by
  party_term_candidates, so no other changes are required for
  apostrophe-only mismatches.


────────────────────────────────────────
Review complete
2 findings ✔

Major    2
────────────────────────────────────────

Print all AI prompts: coderabbit review --show-prompts
```
