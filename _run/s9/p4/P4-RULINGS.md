# P4 orchestrator rulings (fable)

## RULING P4-01 (2026-07-20) — pair-list scope
B-PAIR's superset ADOPTED: the R6 sweep covers case-anchored pairs (case page = canonical
record vs each home — treatment-identity + N4 tag legs) AND home×home C(n,2) pairs (boundary-
sentence semantic contradiction legs). 437 pairs = 100% coverage; narrow reading rejected
(would drop the canonical-record reconciliation the O1 spec text made explicit).

## RULING P4-02 (2026-07-20) — pair-sweep review scope
Per-pair review is SCOPED to the pair's shared_items: (i) treatment status + N4 "limited by"
tag identical across the pair; (ii) overruled cases rendered Historical on both; (iii) the
shared point/case's framing on both pages not semantically contradictory (boundary sentences
included); (iv) nothing else — full-page re-review was P1's job. Hits = candidates only;
panel referee (R1) before adjudication for legal-assertion hits.

## RULING P4-03 (2026-07-21) — I5 frontier re-run units
13 frontier units = the 11 S6 base-category lanes with two risk strands split out per the spec:
"digital" = the cat-3 digital-surveillance sub-umbrella (S3 A6), "civil-remedies" = the cat-11
civil-remedies placed nodes (S3 A5/A10). I5 runs: digital + civil-remedies (risk-chosen) +
RANDOM pick foundations-and-the-fourth-amendment ($RANDOM=11365 % 11, logged). Dual-model:
one codex web_search lane + one Claude lane per unit; diff vs _run/s6-candidates/frontier-*
saturation logs; tripwire = any two-key-real gate-passing find those logs do not account for.

## RULING P4-04 (2026-07-21) — callout↔registry deep-equal gate (COH gate-a)
Finding: 0/80 registry `statement` fields deep-equal their home-page [!rule] callouts; the
convention ON DISK (since the S3/S5 builds) is hand-edited paraphrase. The R10(a) spec text
("deep-equals") therefore conflicts with the entire built corpus — a spec-text-vs-build
convention conflict, not 80 individual defects. DISPOSITION: (i) the 14 CONTENT-divergence
rows + 3 no-callout rows (esp. remedy.exclusionary, a doctrine-type node missing its callout)
are REAL candidates — individual triage/adjudication in Wave F; (ii) the 61 cite-divergence
rows: triage as a class — where the registry cite is wrong-or-poorer, mechanical registry-side
note; where the callout is wrong, page fix candidate; NO mass rewrite either direction;
(iii) the systemic deep-equal question ESCALATES to _review-needed/ for a user decision at P5
— P4 does not rewrite 80 verified statements to satisfy a gate the build never implemented
(P3 rekey-unsanctioned precedent). The single systemic HIGH row carries this disposition.

## RULING P4-05 (2026-07-21) — LINT-12 stale-re-projection class (COH gate-c)
136 of 151 LINT-12 highs are pure re-projection staleness (opinion_id null -> lead_opinion_id
+ stale projected_at; no content change). SANCTIONED as ONE mechanical fix packet: re-run the
S5 projector over the affected pages, verify byte-diff limited to the projected fields, no
legal judgment involved. The 15 substantive rows go to individual Wave-F triage. LINT-14
green; A13 REVIEW-rows vacuous — accepted as reported.

## RULING P4-06 (2026-07-21) — I1-PREP ambiguities
Accepted as built: consolidated per-category cites:(opinion-ids) queries; fixed
filed_after:2026-07-04 floor; key-case scoring cap 15/category with cuts recorded. Entick
(no CL record) noted as structurally unmonitorable — goes in the R12 maintenance handoff.
