# fixture registry (S3 R4 shape)
schema:
  version: "s3.points.registry.v1"
  status_enum: [draft, verified]

nodes:

  - id: fixture.doctrine.rule
    label: "Fixture doctrine rule"
    statement: "The fixture black-letter rule statement binds here."
    home_page: "content/searches/Fixture Doctrine.md"
    also_on: []
    status: draft
    seed: false
    why: "fixture — R6(b)"
