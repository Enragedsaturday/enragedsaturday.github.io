schema:
  version: "test"
bound:
  - s2_point: "node.alpha.one"
    row_type: point_override
    nodes:
      - id: node.alpha.one
    note: "a non-string (mapping) node id must be rejected, never silently dropped"
pending: []
