schema:
  version: "test"
bound:
  - s2_point: "node.alpha.one"
    row_type: point_override
    nodes: ["node.alpha.one"]
    note: "live"
pending:
  - pending_slug: true
    case: "Some Case"
    cluster_id: 999
    expected_field_ii: limited
    nodes: ["node.beta.two"]
    note: "pending"
