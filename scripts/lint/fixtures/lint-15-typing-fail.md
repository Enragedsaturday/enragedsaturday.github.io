---
title: "LINT-15 fail-closed typing fixture (fail)"
type: widget
status: verified
---

# LINT-15 fail-closed typing fixture (fail)

F-S5-10: a content page whose `type` is missing or unrecognized (`widget`) cannot
be classified by LINT-15 and is a HIGH finding — a mistyped doctrine page must
not silently bypass the skeleton check.
