# S8 wrapper — Legal-Term & Case Linking + Glossary (paste into a fresh thread)

> **⚠ SUPERSEDED by the signed spec — `_overhaul2/specs/S8-linking-glossary.spec.md` (APPROVED
> 2026-07-04). Where this wrapper and the spec disagree, the SPEC TEXT WINS** (RUNBOOK §0 precedence
> stack; COH-26 banner convention — added at the coherence pass, 2026-07-04).


You are running **CSSI Overhaul-2 spec S8 — Legal-Term & Case Linking + Glossary** in a clean
context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + lifecycle + findings) and its **S8 entry in §4**
   (including the Audit-inputs block — COH-15, NUM-04, NUM-02).
2. `_overhaul2/PRACTICES.md` (esp. §1 single-source-of-truth/transclusion, §7 signaling, §8
   consistency machinery).
3. The signed specs you conform to: `S1-standards.spec.md` (**R11 term register + single-source
   transclusion — the canonical-node rule S8 mechanizes**; A2's pipeline-vocab ban governs what a
   link caption may say), `S2-authority-database.spec.md` (canonical names + frontier stubs — your
   name-resolution source), `S3-taxonomy.spec.md` (the point registry whose `statement` nodes you
   transclude; R12), `S4-platform-ui.spec.md` (popover/anchor mechanism you ride),
   `S5-entry-models.spec.md` (**R16 pinpoint deep-links — quoted propositions link `#^pin-N`
   anchors; paraphrases link page-level**; R2's `^rule-` anchors are your transclusion sources),
   `S6-coverage-ingest.spec.md` (**R11 — `_run/s6-coverage-ledger.json` is YOUR input: S8 links
   every mention whose caption has a page and applies its own rule to the rest, reading the
   ledger; A1's planning-time candidates are in it**), `S7-doctrine-production.spec.md` (final
   text you link over; **R5 tier-3 paraphrase-downgrades have no pin anchor by design — those
   propositions link page-level, never to an invented anchor**; LCD bullets name un-paged cases
   plainly — the ledger, not a prose label, tracks them).

## Then run the S8 lifecycle (RUNBOOK §1 — research + examples continuous/interactive)
Research (up front and live: `LINT-5`/`LINT-7` behavior, the popover mechanism, Quartz transclusion
embeds, `_overhaul/ledger/S7-term-map.md`, the 388-mention seed) → **show, don't tell** (a real
page rendered at the proposed link density on :8080 — term links + case links + hover previews +
one transcluded rule node; revise live as answers land) → interview (options + open-ended,
recommendation-first) → follow-up research → **self-interview (required, visible) — adversarially
deep on the 1–2 shaping decisions** (likely: the term-of-art inclusion test + every-occurrence vs
first-occurrence density; and the transclusion-vs-restatement boundary at scale) → write
`_overhaul2/specs/S8-linking-glossary.spec.md`. Update RUNBOOK §7 (S8 → ✅), then hand back the
S9 wrapper.

## Key S8 context
- **Mission:** link **every named case** anywhere (even short names) to its page; deep-link
  pinpoints when discussing a passage (S5 R16); **liberal** term-of-art backlinks to Common Legal
  Terms with hover previews (user leans every-occurrence); single-source transclusion of
  canonical rule/term nodes (S1 R11 / S3 R12 / S5 R2 anchors); audit + expand the glossary from
  finalized S7 text (non-vernacular only).
- **The 388 seed:** 388 distinct cases with ≥1 bare mention (NUM-04, confirmed 40/40 sample). S6
  authors the no-page subset; **you link ALL 388+ reading the S6 R11 ledger** — the numbers
  reconcile row-by-row (COH-15). Never resurrect a `removed`/`unverifiable` caption as a link.
- **NUM-02:** pipe-escaping work list — 18 files / 69 affected table lines (per-file list in the
  audit report) — wikilinks inside tables need `\|` display syntax; your link pass owns it.
- **S7 handoffs:** tier-3 paraphrase-downgrades → page-level links (no pin exists, by design —
  spec R5); the S6 A1 planning-time candidates will have pages/ledger states by your wave;
  glossary expansion reads S7's final text only (it is finalized by your wave-3 slot).

## Interview-extract (what to lock)
The term-of-art inclusion test (what earns a glossary link vs reads as plain English); every-
occurrence vs first-occurrence-per-page (user leans every — confirm + set the table/heading
exemptions); highlight/pinpoint policy on deep links; transclusion scope (which rule/term nodes
embed vs link); link density on tables and LCD bullets. *Show:* one page at proposed density +
a transcluded rule node + hover previews, live on :8080.

## Audit intake (REQUIRED — RUNBOOK §7 AUDIT-CLOSURE gate)
Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your rows (`injected:S8` + any row naming S8 —
**COH-15** (link all 388 once pages exist; reconcile 388 / ~84 / LINT-5 jointly with S6's ledger)
· **NUM-04** (the 388 seed confirmed) · **NUM-02** (pipe-escaping list)), and address each —
adopt / adapt / reject-with-rationale — in the spec's Decision Log.

## Deliverable
Write `_overhaul2/specs/S8-linking-glossary.spec.md` (O1 template: Objective · Scope ·
Requirements w/ `*Check:*` · Lessons · Method · Deliverables · Acceptance · Verification · Open
items · Decision log). Update RUNBOOK §7 status (S8 → ✅). Then hand back the S9 wrapper.
