# S9 wrapper — Verification Pipeline & Release Gate (paste into a fresh thread)

You are running **CSSI Overhaul-2 spec S9 — Verification Pipeline & Release Gate** in a clean
context. S9 is the LAST spec; the coherence pass over all nine + the AUDIT-CLOSURE gate follow it.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + lifecycle + findings) and its **S9 entry in §4**
   (including BOTH input blocks — the Audit-inputs block AND the S7-interview inputs (a)–(e)).
2. `_overhaul2/PRACTICES.md` (esp. §3 the 10 gates, §6 guardrails G1–G10) +
   `_overhaul2/CL-DATA-INVENTORY.md` (S9 reads from the lake; barely touches live CL).
3. ALL EIGHT signed specs — S9 verifies everything: `S1-standards.spec.md` (R12 panel + R13/A5
   lint roster — **you codify the final numbering, incl. the `LINT-S2-*`→LINT-12/13/14
   re-pointing A5 promised but S2's text never executed** — closure-verifier finding),
   `S2-authority-database.spec.md` (R15 gates; A8 provisional-slug recheck; **A14 pinpoint
   `fragment` field — S8's write-back**), `S3-taxonomy.spec.md` (registry/binding lints),
   `S4-platform-ui.spec.md` (R8 retirement checklist post-EXECUTE; R5/R6 sampled),
   `S5-entry-models.spec.md` (LINT-15/16; R2 callout↔registry coherence; ≥2-reviewer rule
   layer), `S6-coverage-ingest.spec.md` (R11 ledger partition; LINT-17; ≥1-in-10 gate re-verify
   via the Claude lane — COH-17), `S7-doctrine-production.spec.md` (R3/R4/R5 re-verification by
   tier; per-item G2; the cross-page contradiction sweep), `S8-linking-glossary.spec.md`
   (**R12/R13 — see the S8 handoffs below**).

## Then run the S9 lifecycle (RUNBOOK §1 — research + examples continuous/interactive)
Research (up front and live: O1's `S9-verification.spec.md` + findings/adjudications ledgers in
`_run/` and `_overhaul/`; the headless-Codex reviewer recipe in RUNBOOK §6 incl. the COH-31
timeout note; the existing `scripts/lint/` roster) → **show, don't tell** (the finding/
adjudication JSON schema + ONE worked finding driven through find→adjudicate→fix→re-review, live)
→ interview (options + open-ended, recommendation-first) → follow-up research → **self-interview
(required, visible)** — adversarially deep on the 1–2 shaping decisions (likely: exhaustive vs
sampled forced primary re-reads; and how the two Codex lanes stay genuinely independent of each
other and of the builder) → write `_overhaul2/specs/S9-verification.spec.md`. Update RUNBOOK §7
(S9 → ✅). Then hand back to the coherence pass (no further wrapper).

## Key S9 context
- **Mission:** the continuous fail-closed pipeline + the release gate: writer ≠ checker; 1 Claude
  + 2 Codex adversarial panel, real ≥2-of-3 refute tally; the 10-gate protocol; a machine-emitted
  reconcilable ledger; forced primary reads on recent/high-profile cases; blind re-derivation;
  post-fix re-review by a non-author model; the full CI lint roster, fail-closed. Reads from the
  S2 lake (one serial lane per credential — S1 A1/L4′; the ≥1-in-10 identity spot-check routes to
  the **Claude** lane, COH-17).
- **The question checklist:** framed? explained? cited? related? narrowed? expanded? good law?
  abrogated/overruled? current treatment? — run per entry.
- **Codex recipe:** headless `codex exec -s workspace-write -c approval_policy=never
  --skip-git-repo-check --json -C <dir> -o <last.txt> "<prompt>" < /dev/null`, model gpt-5.5,
  caller-side timeout (macOS has no GNU timeout — COH-31). Codex reviewers read the data-lake;
  no CL MCP.

## S8 handoffs (2026-07-04 — from `S8-linking-glossary.spec.md`; treat like audit rows)
- **NUM-03 boundary (ADAPTED, logged in S8's Decision Log):** S8 R6 executes the mid-line
  `^pin-N` **content remediation** (S8 touches those blocks to wire deep links); **S9 keeps
  LINT-9** (carat-leak guard, sized ~230 files for verification) **+ re-verification** that no
  visible carat survives and no pin link broke.
- **Lint kit to wire + number** (S1 A5/COH-21 roster codification): rewritten LINT-5
  (ledger-aware bare-caption rule; markdown-link-text masking; **broken anchors = HIGH,
  fail-closed**; full-slug `![[` embeds), rewritten LINT-7 (first-occurrence rule DELETED —
  inverted by S8 D1; register-coverage review flags), pipe-escape check, fragment
  well-formedness check. Fixtures ship in `scripts/lint/fixtures/`.
- **S8 ledger review:** sample ≥1-in-10 `_run/s8-link-ledger.json` rows via the Claude lane;
  re-review **100% of adjudicated ambiguity resolutions** (the judgment surface — the
  three-Morgans class).
- **Fragment spot-checks:** follow sampled external `#:~:text=` pincites end-to-end (the
  highlight must land on the quoted passage); fragments must trace to G3-passed lake quotes
  (S2 A14); zero fragments on S7 tier-3 paraphrases.
- **Shingle detector scope:** S1 A3's duplicate-prose detector must EXCLUDE transclusion embeds
  (they are the sanctioned way past it — S8 R9); verify it still fires on raw restatements.
- **R10 visual sample:** deep-link landing = centered + flash + persistent tint, SPA and hard
  loads both.

## Interview-extract (what to lock)
Exhaustive vs sampled forced primary re-reads; refuters per finding; how the two Codex lanes stay
independent (of each other AND of the Codex builder — COH-17's writer≠checker repair); the
definition-of-done for the release gate; ledger schema sign-off. *Show:* the finding/adjudication
JSON schema + one worked finding through the full loop, live.

## Audit intake (REQUIRED — RUNBOOK §7 AUDIT-CLOSURE gate)
Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your rows (`injected:S9` + any row naming S9 —
**COH-17** (Claude-lane identity spot-checks) · **COH-21** (lint-naming standardization before
codifying the roster; NB the closure-verifier finding that S1 A5's "executed there" clause is
false on disk — the re-pointing lands HERE) · **COH-27** (re-poll "pending" markers — *Carter v.
United States* cert-watch No. 25-885; also S6's *Noem* watch row) · **COH-28** (LINT-3 Chatrie
recent-dev false-positive fix into the roster) · **TEACH-02b** (LINT-11 pipeline-vocab build) ·
**TEACH-11** (mnemonic lint needs wikilink-target checks) · **NUM-03** (LINT-9 sized ~230 files;
content half rides S8 — see handoffs) · **S2F-07b** (S7 prose vs provisional point slugs recheck
after the S3 binding map resolves)), and address each — adopt / adapt / reject-with-rationale —
in the spec's Decision Log. Also intake the **S7-interview inputs (a)–(e)** in RUNBOOK §4-S9
(cluster-vs-opinion id resolution · per-item G2 · cross-page contradiction sweep · tier-sampled
TEACH-03 re-verification · LINT-10 block unit) — the S8 thread re-confirmed (a) live: a
`search_document` against a page-URL id returned 0 hits on famous *Jardines* text.

## Deliverable
Write `_overhaul2/specs/S9-verification.spec.md` (O1 template: Objective · Scope · Requirements
w/ `*Check:*` · Lessons · Method · Deliverables · Acceptance · Verification · Open items ·
Decision log). Update RUNBOOK §7 status (S9 → ✅). Then hand back to the coherence pass over all
nine specs (AUDIT-CLOSURE gate) → one autonomous EXECUTE run (data-lake first).
