# S2 wrapper — Verified Authority Database (paste into a fresh thread)

> **⚠ SUPERSEDED by the signed spec — `_overhaul2/specs/S2-authority-database.spec.md` (APPROVED
> 2026-07-01). Where this wrapper and the spec disagree (e.g., the Justia/Scholar off-CL links below
> vs the spec's four-source whitelist), the SPEC TEXT WINS** (RUNBOOK §0 precedence stack; audit
> COH-26). Kept as a historical record only — do not run this wrapper.

You are running **CSSI Overhaul-2 spec S2 — Verified Authority Database** in a clean context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + per-spec lifecycle + findings) and its **S2 entry in §4**.
2. `_overhaul2/PRACTICES.md` (the editorial/verification playbook — esp. §2 treatment vocab, §3 the 10
   verification gates, §4 the comprehensive web+CL research protocol, §6 AI guardrails).
3. `_overhaul2/CL-DATA-INVENTORY.md` (the exact CourtListener fields/calls — the schema seed).
4. Confirm S1 is written: `_overhaul2/specs/S1-standards.spec.md` (the constitution you conform to).

## Then run the S2 lifecycle (RUNBOOK §1)
Research first → interview (options + open-ended, recommendation-first; **show** a fully-populated
example case record incl. its progeny map) → follow-up → write `_overhaul2/specs/S2-authority-database.spec.md`.

## Key S2 context (decided in prior threads)
- **Codex builds the lake**, not Claude (to conserve Claude usage). It runs a **direct CL REST API v4**
  ingest script: token at `~/.config/cssi/cl-token` (~1,000 req/hr → **paced + disk-cached + resumable**);
  headless `codex exec` needs `-c sandbox_workspace_write.network_access=true`. **No MCP.** Codex =
  builder + reviewer; you orchestrate/design + take 1 of 3 review lanes.
- **One record per case:** verified identity (cluster → lead opinion; **disambiguate by citation** — a
  name search can rank a procedural order first), reporter cites + pinpoints, the **3-field treatment
  vocabulary + dual as-of dates** (derived from progeny text + web — **CL has NO treatment signal**),
  the **progeny / citing-references map** (`cites:(<all sibling opinion ids OR'd>)`) = the
  expansion/narrowing picture, **off-CL Justia/Scholar links** for coverage gaps, and a provenance trail.
- **"Not found ≠ fabricated"** — block + investigate, cross-check a second source; compare the input
  case name against CL's canonical name yourself (the auto warning is masked by cite dedup).
- Absorb the Chatrie/Zorn corrupted-object tickets ("never ingest" the corrupted objects).
- On connected storage (not server disk), resumable. Every downstream spec + S9 read from this lake.

## Deliverable
Write `_overhaul2/specs/S2-authority-database.spec.md` (O1 template: Objective · Scope · Requirements w/
Checks · Lessons · Method · Deliverables · Acceptance · Verification · Open items · Decision log).
Update RUNBOOK §7 status (S2 → ✅). Then hand back the S3 wrapper.
