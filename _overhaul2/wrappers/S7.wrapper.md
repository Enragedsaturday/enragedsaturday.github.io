# S7 wrapper — Doctrine Production (paste into a fresh thread)

You are running **CSSI Overhaul-2 spec S7 — Doctrine Production (brief-first rewrite)** in a clean
context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + per-spec lifecycle + findings) and its **S7 entry in
   §4** (including the **Audit inputs** block — your injected IDs, and the surviving per-page
   fix-list that replaced the missing `Prompt.md`).
2. `_overhaul2/PRACTICES.md` (esp. §5 graded-authority layers, §6 guardrails, §8 style machinery).
3. The signed specs you conform to: `S1-standards.spec.md` (constitution — **the officer-BLUF /
   field-application layer is BANNED project-wide, S1 §2.2 + R6**; R7 never auto-generate a
   controlling standard; the em-dash policy; the mnemonic register), `S2-authority-database.spec.md`
   (prose treatment statements reconcile against the lake — S9's coherence gate),
   `S3-taxonomy.spec.md` (the tree + point registry your rewrites hang off; placed-empty nodes get
   their prose from YOU), `S5-entry-models.spec.md` (**the doctrine-page skeleton is signed — R1
   H2 sequence, R2 rule-callout as canonical statement site, R10 pitfalls shape, R11 "Lower-court
   developments" above the tables, R12 bracketed Sources; apply, don't re-interview**),
   `S6-coverage-ingest.spec.md` (**your two handoffs: R8 — the ONE reusable page-authoring pipeline
   you invoke for any rewrite-time case discovery, no second page-mint; R11 — the coverage ledger
   telling you every caption's terminal state; plus R4 — fabrication-removal prose surgery is
   coordinated with you**).

## Then run the S7 lifecycle (RUNBOOK §1 — research + examples are continuous/interactive)
Research (up front **and live mid-interview**; resolve the flagged doctrinal questions with
authority — horizontal pooling vs *Pringle*, the knock-and-talk split, caretaking-of-persons,
Santana's live limits) → **show, don't tell** (ONE fully rewritten doctrine brief as the pattern —
written example first, on the dev server `npx quartz build --serve` :8080; **revise on the spot as
answers land, pivot freely**) → interview (options + open-ended, recommendation-first) → follow-up
research → **self-interview (required, visible) — adversarially deep on the 1–2 shaping decisions**
(likely: the brief template's section order/voice envelope and the exhaustive-vs-budgeted treatment
allocation across ~22 doctrine pages) → write `_overhaul2/specs/S7-doctrine-production.spec.md` +
the per-page change-list. Update RUNBOOK §7 (S7 → ✅), then hand back the S8 wrapper.

## Key S7 context
- **Mission:** produce/rewrite every doctrine/narrative page through draft→review→verified:
  brief-first (rule + tests up front + limits + nuance + pitfalls integrated, no "(woven in)"
  labels); numbered apply-it lists; nothing after the tables except the diagram; fix slip-op
  pinpoints (TEACH-03: 43/47 case pages + ~22 doctrine pages), pipe-escaping, `^pin-N` leaks.
- **`Prompt.md` is MISSING** (COH-14): not in the repo anywhere; RUNBOOK §4-S7's parenthetical list
  is the surviving record — **regenerate or drop the reference at the interview** (S5 dropped its
  COH-14 PDF; precedent exists).
- **Officer summaries do not exist** (S1 ban); existing "field framing" prose migrates per
  TEACH-04e (define + apply the delete-vs-convert-to-N3-list rule).
- **S6 → S7 handoff:** every named-in-prose case now has a ledger terminal state — your rewrites
  wikilink what has pages and never resurrect a `removed`/`unverifiable` caption; a case you
  discover mid-rewrite goes through S6 R8's pipeline (born-conformant page), never hand-minted.
  The S5 R11 bullets you write feed the D5 bullet→page conversion rule when prose comes to rely on
  a bullet case.
- Weave mnemonics where they earn it (the verified register only); Humanizer voice per S1;
  black-letter rules are the ≥2-reviewer layer (S9 mechanics).

## Interview-extract (what to lock)
The brief template + section order sign-off (within S5 R1's skeleton); which topics get exhaustive
treatment vs the ½–1-page budget; voice heaviness; the TEACH-04e migration rule's exact shape; the
`Prompt.md` regenerate-or-drop call; the GAP-03b emerging-tech cluster scope inside the
Third-Party/Digital rewrite (reverse-keyword H · StingRay H · real-time CSLI M · IGG M · BWC M) +
GAP-03c (§702 / parallel construction mention). *Show:* one fully rewritten doctrine brief.

## Audit intake (REQUIRED — RUNBOOK §7 AUDIT-CLOSURE gate)
Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your rows (`injected:S7` + any row naming S7 —
**TEACH-01, TEACH-02c, TEACH-03 + NUM-01, TEACH-04a–h, TEACH-05, TEACH-08, TEACH-12a/b, GAP-03b,
GAP-03c, GAP-06 (verify no residue), LAW-05, NUM-08 (negative scope — budget no work)**), and
address each — **adopt / adapt / reject-with-rationale** — in the spec's Decision Log.

## Deliverable
Write `_overhaul2/specs/S7-doctrine-production.spec.md` (O1 template: Objective · Scope ·
Requirements w/ `*Check:*` · Lessons · Method · Deliverables · Acceptance · Verification · Open
items · Decision log) + the per-page change-list. Update RUNBOOK §7 status (S7 → ✅). Then hand
back the S8 wrapper.
