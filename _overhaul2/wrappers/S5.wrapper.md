# S5 wrapper — Entry Models (paste into a fresh thread)

> **⚠ SUPERSEDED by the signed spec — `_overhaul2/specs/S5-entry-models.spec.md` (APPROVED
> 2026-07-03). Where this wrapper and the spec disagree, the SPEC TEXT WINS** (RUNBOOK §0 precedence
> stack; audit COH-26). Kept as a historical record. Note: the COH-14 missing book PDF was resolved
> at interview — reference **dropped** (spec Decision Log D12).

You are running **CSSI Overhaul-2 spec S5 — Entry Models (case + doctrine pages)** in a clean context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + per-spec lifecycle + findings) and its **S5 entry in §4**
   (including the **Audit inputs** block — your injected IDs).
2. `_overhaul2/PRACTICES.md` (esp. §5 graded-authority entry model, §7 reader-facing signaling).
3. The signed specs you conform to: `S1-standards.spec.md` (constitution — note **R6 Variant A is
   already adjudicated; do NOT re-mock or re-interview the graded-authority depth — audit COH-12**),
   `S2-authority-database.spec.md` (the frontmatter projection you render), `S3-taxonomy.spec.md`
   (tree + points), `S4-platform-ui.spec.md` (**your mechanism supplier — COH-18: S4 owns the
   pill/anchor/hover/popover MECHANISM; S5 owns pill PLACEMENT/format inside the entry models**).

## Then run the S5 lifecycle (RUNBOOK §1 — research + mockups are continuous/interactive)
Research (up front **and live mid-interview**) → **show, don't tell** (browser mockup of the new
case table + pill placement + one written BIRAC page on a branch — dev server
`npx quartz build --serve` :8080; **revise on the spot as answers land, pivot freely**) → interview
(options + open-ended, recommendation-first) → follow-up research → **self-interview (required,
visible) over the self-owned design decisions — adversarially deep on the 1–2 that shape everything
downstream** → write `_overhaul2/specs/S5-entry-models.spec.md`. Update RUNBOOK §7 (S5 → ✅), then
hand back the S6 wrapper.

## Key S5 context
- **BIRAC case page kept** (liked in O1). Graded-authority doctrine page = **S1 R6 Variant A**
  (black-letter rule / explanation / authorities) — settled, build to it.
- **The officer-bottom-line / "In the field" box is BANNED project-wide** (S1 §2.2 + R6 — audit
  COH-01): do not design, mock, or spec it.
- Standardized case-table schema: short shared headers (**51 distinct "Case" header schemas → 1**,
  NUM-07), controlled widths, no side-scroll; treatment **pill under the case name**; **weight in
  the case column**; treatment column + "treated in full" removed; relevance tags under relevance;
  "limited by/expanded by" folds into the holding.
- The pill is an anchor to *Verifying Good Law* with hover preview — **S4 ships the mechanism +
  the shared target constant (`caseHelpers.ts`) + popover delegation; you decide where pills sit
  and what they show in tables** (S4 R5/R6 tooltip template is the default; extend, don't fork).
- Draft-state machinery (`draft → under_review → verified`) rendering; provenance rendering.
- **Missing input (audit COH-14):** the book case-example PDF `.orca/drops/Book Jun 26, 2026 (1).pdf`
  is not on disk — **regenerate or drop at the interview** (only `Book Jun 26, 2026.pdf` exists).

## S4 → S5 handoff (important)
S4's working standard is live on `overhaul2/planning` (mockup commits `8655398`, `37d6f4f`,
`bd50770`; spec = the authority). You inherit: the TOC-tree nav (whole-header category toggle +
Overview first-child row — user decision D1), the pill-as-anchor mechanism with **event-delegated
popovers** (any anchor you inject in tables gets hovers for free), the provenance tooltip template
(status · as-of/dual dates · note · methodology pointer — reads S2's projection, degrades to
`as_of`), and the weight-reading explorer sort (`ContentDetails.weight`). **Dates never render
inline in prose** (PRACTICES §7 placement decision) — your table/entry designs must respect that.

## Interview-extract (what to lock)
BIRAC section order + voice; final table columns + the one header schema (NUM-07); the as-of page +
hover presentation *within entries*; where deep-link/pinpoint highlights apply; Sources-section
format (TEACH-14); treatment-cell format unification (TEACH-15 — decide whether table cells count
as "inline" under S1 R3); "Recent developments" heading standard (TEACH-08); pitfalls format
(TEACH-09); grouped Key-cases sub-tables y/n (TEACH-10). *Show:* the mockup table + pill + hover on
the dev server; one written BIRAC page.

## Audit intake (REQUIRED — RUNBOOK §7 AUDIT-CLOSURE gate)
Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your rows (`injected:S5` + any row naming S5 —
TEACH-08/09/10/13/14/15, NUM-07, COH-11, COH-18, COH-14), and address each — **adopt / adapt /
reject-with-rationale** — in the spec's Decision Log. COH-11: consume the S1/S2 old→new
treatment-enum migration mapping before the projector overwrites.

## Deliverable
Write `_overhaul2/specs/S5-entry-models.spec.md` (O1 template: Objective · Scope · Requirements w/
`*Check:*` · Lessons · Method · Deliverables · Acceptance · Verification · Open items · Decision
log). Update RUNBOOK §7 status (S5 → ✅). Then hand back the S6 wrapper.
