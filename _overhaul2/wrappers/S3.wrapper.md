# S3 wrapper — Taxonomy & Points-of-Law (paste into a fresh thread)

> **⚠ SUPERSEDED by the signed spec — `_overhaul2/specs/S3-taxonomy.spec.md` (APPROVED 2026-07-02).
> Where this wrapper and the spec disagree, the SPEC TEXT WINS** (RUNBOOK §0 precedence stack; audit
> COH-26). Kept as a historical record **and as the template future wrappers (S4+) copy** — see the
> template notes at the end.

You are running **CSSI Overhaul-2 spec S3 — Taxonomy & Points-of-Law** in a clean context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + per-spec lifecycle + findings) and its **S3 entry in §4**.
2. `_overhaul2/PRACTICES.md` (the editorial/verification playbook — esp. §1 taxonomy-as-spine +
   single-source-of-truth, §8 the term register + transclusion rule).
3. Confirm the constitution + the spine you conform to: `_overhaul2/specs/S1-standards.spec.md` and
   `_overhaul2/specs/S2-authority-database.spec.md` (both written).

## Then run the S3 lifecycle (RUNBOOK §1 — research + mockups are continuous/interactive)
Research (up front **and live mid-interview**) → **show, don't tell** (a **live nav mockup on a branch**
— dev server `npx quartz build --serve` :8080 — plus one written **overview page**; **revise the mockup
on the spot as answers land, pivot freely**; lock the layout on the mockup before writing) → interview
(options + open-ended, recommendation-first) → follow-up research → **self-interview (required, visible)
over the self-owned design decisions — adversarially deep on the 1–2 that shape everything downstream**
→ write `_overhaul2/specs/S3-taxonomy.spec.md`.

## Key S3 context
- **Our own category tree**, 3-level nesting (index-1 umbrella w/ authored **overview page** →
  index-2 doctrine/sub-umbrella → index-3 doctrine leaf), self-explanatory, each node self-contained,
  more granular where it earns it. The taxonomy **is the product** — one controlled classification is
  the spine every proposition hangs off (PRACTICES §1). **Do NOT plagiarize the book.**
- Draft moves to refine (inputs, not decisions): Exigency → flavors; relocate **Plain View** to a
  seizure cluster; promote **Checkpoints / Inventory / Border** out of the Special-Needs mega-page;
  rename **"Levels of Suspicion" → "Standards of Proof"** (full burden pyramid, **PC < preponderance**);
  re-home **Brendlin / Katz / Graham**; **unlist `cases/`**; the possible **"Home Entry & Search"** topic.
- **Research inputs:** current tree + Bandiero TOC + **LaFave TOC** (`.orca/drops/SSTOC.pdf`) + **NJ
  Handbook** (`.orca/drops/New Jersey Law Enforcement Handbook…pdf`) + Wex/casebook order + the draft
  tree (`~/briefs/2026-07-01-cssi-o2-taxonomy-proposal.html`, an input only).

## S2 → S3 handoff (important)
S2's **treatment `point_overrides`** carry **provisional `point` slugs** with
`s3_binding_status: "provisional"`. S3's controlled point-of-law nodes are the authority those slugs
bind to — **S3 must define the taxonomy node IDs and a `point → S3 node` binding map** (1:N / N:1
allowed), after which a fail-closed lint activates ("every `point_override.point` must bind"). Design
the point-of-law granularity with that binding in mind (e.g., Belton's "vehicle search incident to a
recent occupant's arrest" override needs a node to land on). The taxonomy is also where per-page
treatment routing resolves (which override renders on which page).

## Interview-extract (what to lock)
Umbrella set + names; exceptions axis (suspicion-tier vs object vs flat); **"Home Entry & Search"** y/n;
**"Standards of Proof"** rename; nav size; **Knock-and-Talk** placement; which nodes are index-2
umbrellas vs index-3 leaves; per-case re-homings; **the point-of-law node granularity + the S2 binding
map**. *Show:* the live nav mockup + a written overview page — lock the layout on the mockup first.

## Deliverable
Write `_overhaul2/specs/S3-taxonomy.spec.md` (O1 template) — target tree + overview list + re-homing
table + the point-of-law node scheme + the `point → S3 node` binding map for S2's overrides.
Update RUNBOOK §7 status (S3 → ✅). Then hand back the S4 wrapper.

---

## Template notes for future wrappers (S4+) — audit COH-25
Every future wrapper copies this file and MUST carry these two lifecycle steps (see also RUNBOOK §0
"Wrapper template notes"):
1. **Self-interview — REQUIRED, visible, before writing** (RUNBOOK §1 step 5): after the user's
   decisions, pose each self-owned design decision as an explicit question, argue the alternatives,
   pick with rationale, and go adversarially deep on the 1–2 decisions that shape everything
   downstream. It becomes the spec's Decision Log.
2. **Audit intake:** "Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your spec's rows (its
   `injected:` IDs — mirrored in your RUNBOOK §4 entry's **Audit inputs** block — plus any row naming
   your spec), and address each — **adopt / adapt / reject-with-rationale** — in the spec's Decision
   Log." The RUNBOOK §7 AUDIT-CLOSURE gate fails the coherence pass if any injected ID lacks a
   disposition.
