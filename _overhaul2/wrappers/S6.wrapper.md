# S6 wrapper — Coverage & Ingest (paste into a fresh thread)

> **⚠ SUPERSEDED by the signed spec — `_overhaul2/specs/S6-coverage-ingest.spec.md` (APPROVED
> 2026-07-03). Where this wrapper and the spec disagree, the SPEC TEXT WINS** (RUNBOOK §0 precedence
> stack; audit COH-26). Kept as a historical record. Note: the wrapper's "author a BIRAC page for
> the real ones" is now governed by the spec's R2 gate + user D1 flip; the "~80–84" figure was
> superseded by the committed 89-row SEED (88 live after the specimen).

You are running **CSSI Overhaul-2 spec S6 — Coverage & Ingest (verify + author missing cases)** in a
clean context.

## Read first (grounding)
1. `_overhaul2/RUNBOOK.md` §0–§2 (how-to-use + per-spec lifecycle + findings) and its **S6 entry in
   §4** (including the **Audit inputs** block — your injected IDs).
2. `_overhaul2/PRACTICES.md` (esp. §3 the 10 gates, §4 the comprehensive-research protocol, §6
   guardrails — "not found ≠ fabricated").
3. **`_overhaul2/S6-SEED.md`** — your committed roster (89 entries, regenerated per audit COH-02a;
   `_overhaul2/scripts/audit_cases.py` is its generator). Consume it as the seed, not as gospel.
4. `_overhaul2/CL-DATA-INVENTORY.md` (what CL gives you — identity/fabrication checks ride S2's
   machinery).
5. The signed specs you conform to: `S1-standards.spec.md` (constitution — the officer-BLUF layer is
   **banned project-wide**, S1 §2.2 + R6), `S2-authority-database.spec.md` (**S2 pre-seeds your
   roster with identity-verified frontier stubs + fabrication flags — R11/A6; you author pages for
   the real ones and promote the stub record_id**), `S3-taxonomy.spec.md` (the tree your new pages
   land in — placed-empty nodes are yours to fill, R7), `S5-entry-models.spec.md` (**every page you
   author is born conformant: BIRAC skeleton, 3-field treatment rendering, sanctioned table schemas,
   bracketed Sources, `opinion` anchor text, no authored data in table cells**).

## Then run the S6 lifecycle (RUNBOOK §1 — research + examples are continuous/interactive)
Research (up front **and live mid-interview**) → **show, don't tell** (the missed-case list +
why-missed taxonomy on a sample; one fully-authored specimen page from the roster) → interview
(options + open-ended, recommendation-first) → follow-up research → **self-interview (required,
visible) — adversarially deep on the 1–2 shaping decisions** (likely: the relevance-gate threshold
mechanics and the frontier-reach stopping rule) → write `_overhaul2/specs/S6-coverage-ingest.spec.md`.
Update RUNBOOK §7 (S6 → ✅), then hand back the S7 wrapper.

## Key S6 context
- **Mission:** verify + author the **named-but-no-page** cases. Roster = `_overhaul2/S6-SEED.md`
  (89; ~61 gov-caption, mostly circuit/state). **Verify existence FIRST** (two-key; "not found ≠
  fabricated"; compare input name vs CL canonical) — author a BIRAC page + external link for the
  real ones, **remove** fabrications (*Mayville/Small/Lyle/Moore-Bush* were flagged in O1).
- **The 388/89 split (audit COH-15):** 388 distinct cases have ≥1 bare mention (NUM-04, confirmed);
  **S6 owns existence-verification + authoring of ONLY the no-page subset** (the SEED roster);
  **S8 links ALL 388** once pages exist. Reconcile the numbers jointly with S8 in your spec.
- Diff the corpus against **book roster ∪ named-in-prose ∪ prior-research ∪ bounded frontier**, with
  an officer-field-relevance gate. Fix ~6 alias/variant mismatches; ignore ~5 citation-format
  placeholders.
- **Human pauses that touch you** (RUNBOOK §0 register): fabrication removals (#2) and
  borderline-relevance sign-off (#3) are enumerated user pauses — design them into your pipeline.
- **S2 boundary:** S2's builder owns the CL project token (one lane per credential — S1 A1/L4′).
  Your existence checks at EXECUTE read S2's stubs/lake, not live CL; interactive spot-checks only
  via the Claude MCP lane.
- **S5 boundary:** page format is decided — do not re-interview BIRAC, tables, pills, or Sources;
  new pages simply conform (S5 R3/R6/R7/R12; born with `lake.status` + banner behavior per S5 R15).

## Audit intake (REQUIRED — RUNBOOK §7 AUDIT-CLOSURE gate)
Read `_overhaul2/AUDIT-2026-07-02.md`, filter to your rows (`injected:S6` + `injected:S6+S8` + any
row naming S6 — **GAP-01b, GAP-02b, GAP-04a–g, GAP-05, COH-15**, plus the S6-SEED consumption per
COH-02a/NUM-05), and address each — **adopt / adapt / reject-with-rationale** — in the spec's
Decision Log. Highlights:
- **GAP-01b/02b:** author retaliatory-arrest (*Nieves* 2019, *Gonzalez* 2024) and 4A
  malicious-prosecution (*Thompson* 2022, *Chiaverini* 2024) coverage — S3 A5 reserved the cat-11
  nodes.
- **GAP-04a–g:** *Cooley* · *Lombardo* · *Culley* · reconsider *Egbert* · *Noem v. Vasquez Perdomo*
  (watch) · assess *Villarreal* (likely reject-with-rationale) · reconsider *Martin* (FTCA).
- **GAP-05:** add an **OT2019→present term-by-term SCOTUS sweep** as a named seed source (the
  named-in-prose leg is structurally blind to never-named doctrine); recurring version → GH#2.

## Interview-extract (what to lock)
Relevance-gate threshold (what's "officer-field-relevant" enough to earn a page); frontier reach +
stopping rule (how far past the seed the expansion goes — saturation per PRACTICES §4);
borderline sign-off mechanics (batch vs per-case); the why-missed taxonomy; GAP-04 case-by-case
dispositions; how the GAP-05 term sweep is bounded. *Show:* the roster + why-missed→
generalized-search on a sample; one authored specimen page.

## Deliverable
Write `_overhaul2/specs/S6-coverage-ingest.spec.md` (O1 template: Objective · Scope · Requirements
w/ `*Check:*` · Lessons · Method · Deliverables · Acceptance · Verification · Open items · Decision
log). Update RUNBOOK §7 status (S6 → ✅). Then hand back the S7 wrapper.
